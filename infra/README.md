# Infrastructure (Phase 5)

AWS deployment configuration will live here:

- Terraform for RDS, ECS Fargate, ALB, Secrets Manager
- Docker image build for the Django backend
- Vercel project settings documented in the root README

Deferred until Phase 5.
